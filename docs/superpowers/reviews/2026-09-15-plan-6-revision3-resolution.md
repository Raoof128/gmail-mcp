# Plan 6 revision 3 review resolution

This revision addresses B1–B6 and M1–M9 in the supplied follow-up review. It changes planning documents and their executable contract fixture. Production modules remain unchanged. Revision 2 is preserved at `b7c06e4`; earlier manifests describe the artifacts at their original commits.

| Finding | Correction and verification                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1      | Consumption includes authorization, preparation, sample, slot, resolved intent, template, argument hashes and byte count. Both outcomes return the committed record. Checks reject hash substitution. Task 4 must prove atomic persistence and two-process admission.                                                                                                                                                           |
| B2, M6  | workerObserve/nativeObserve have read-only operation allowlists; workerMutate/nativeMutate require opaque MutationAdmission. Negative TypeScript fixtures reject null admissions and mutation calls through read APIs. Native actions and remote upload/ACK each consume separate slots.                                                                                                                                        |
| B3      | ToolCapability and validateResolvedMutation check the allocated slot, authorization, target/grant, template, request digest/bytes and exact sender/To with empty CC/BCC. Positive and negative fixtures cover native capabilities and recipient/tool substitutions. Runtime adapters must derive projections from actual shared-schema requests and effective MIME/draft recipients; caller projections cannot grant authority. |
| B4      | Seven strict purpose/phase shapes replace the nullable envelope. Disable requires a concrete expected epoch. Probe permits explicit null only as an atomic absence assertion; run/enable require a concrete probe epoch. Checks cover all valid shapes, phase contamination, duplicate IDs and mixed modes.                                                                                                                     |
| B5      | RestoreTarget requires database and generation equality with Snapshot. A new negative check failed against revision 2 and passes after the fix. Generation identifies the already rotated external value, not a proposed next value.                                                                                                                                                                                            |
| B6      | P6-AUDIT-HEADER accounts for the retired item. The current original design section 6 already retires it; the correction preserves that decision and expands Task 12 coverage to retired entries.                                                                                                                                                                                                                                |
| M1      | credentialVersion explicitly means Google grant identity. Current tokens.ts preserves it during refresh; connect.ts and revoke increment it. No production rename or migration is needed.                                                                                                                                                                                                                                       |
| M2      | Awaited component orchestration commits preparation, fixes identity, records observation/source, records component-ready source, closes outcomes and emits the report. Checks cover order and interrupted observation; Task 4 must verify the complete source graph and durable crash behavior.                                                                                                                                 |
| M3      | Bounded descriptor reads precede fatal UTF-8 decode, JSON parse, depth/key checks and schema parsing. The reference parser rejects oversized, invalid UTF-8 and excessively nested artifacts. Large payloads stay in separate streaming fixtures.                                                                                                                                                                               |
| M4      | Resource details require counted request/response, attachment and MIME bytes plus fixture SHA. Schema checks reject a small payload or a boolean-only claim; Task 10 must derive these values from actual streams and verify the measurement source.                                                                                                                                                                            |
| M5      | Reply/revoke observations carry sacrificialTargetHash and resolvedIntentSha256. Precommitted targetScope permits only reply/revoke on the authorized sacrificial account/grant. Checks reject grant substitution. Runtime source verification must join these fields to the consumed slot.                                                                                                                                      |
| M7      | Task 11 specifies two separate mode proof runs and nine independent common component runs, with separate identities/manifests and mode epochs.                                                                                                                                                                                                                                                                                  |
| M8      | Restore export names operation-journal and idempotency records. It excludes Worker secrets, TOKEN_KEK and credential material; any credential backup requires a separate design.                                                                                                                                                                                                                                                |
| M9      | Parsed the historical CSV: 136 rows, 129 distinct body hashes, seven additional sites sharing bodies. All stored SQL strings reproduce their SHA-256. This verifies inventory counts, not execution coverage. Task 3 still owns parameter binding and per-site outcomes.                                                                                                                                                        |

The inventory is `docs/superpowers/reviews/2026-09-13-plan-5-writer-inventory.csv`, SHA-256 `11990854f4b0dd41835d27e7740787019c7c56de229ba007ccb0dd431db1cbca`. Its baseline remains `6e78bc548c97bbe5194e3f780c717e5519930c1a`. The revision manifest includes the normative Plan 6 design, original design and inherited Plan 5 contracts so reviewers can retrieve the full referenced chain. This correction pass does not claim a new line-by-line certification of that whole chain.

Validation: `node docs/superpowers/reviews/2026-09-15-plan-6-contract-check.mjs` passes 28 checks and strict TypeScript compilation, including negative authority API fixtures. `npm run verify` exits 0 with 358 tests passing (shared 11, Worker 313, companion 9, qualification 25). The initial lint run caught an undeclared TextEncoder; the checker now imports it from node:util. Formatting and diff checks pass. Native tests were not rerun because native code did not change.

Runtime implementation remains pending, with provider-barrier, writer-quiescence, and peak-memory feasibility gates explicitly retained.
